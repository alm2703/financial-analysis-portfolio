import SiteHeader from "../../components/SiteHeader";import SiteFooter from "../../components/SiteFooter";import {getAnalysis} from "../../lib/content";
export const metadata={title:"Analysis — Financial Analysis & Modeling",description:"Shorter research on companies, industries, trends, and financial metrics."};
export default function Analysis(){const items=getAnalysis();return <main><SiteHeader/><header className="page-intro shell"><p className="eyebrow">Research notes</p><h1>Analysis</h1><p className="lede">Shorter work on industries, companies, financial trends, business models, benchmarking, and metrics.</p></header><section className="library shell">{items.map(x=><article className="note" key={x.slug}><div><span>{x.category}</span><span>{x.date}</span></div><h2>{x.title}</h2><p>{x.description}</p><span className="text-link">Read note ↗</span></article>)}<article className="note muted-card"><div><span>Research queue</span></div><h2>More analysis is being prepared.</h2><p>New notes can be added as Markdown files in the analysis content folder.</p></article></section><SiteFooter/></main>}
