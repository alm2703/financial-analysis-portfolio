import type {Metadata} from "next";import SiteHeader from "../../../components/SiteHeader";import SiteFooter from "../../../components/SiteFooter";import {getProject,getProjects,Markdown} from "../../../lib/content";
export function generateStaticParams(){return getProjects().map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const p=getProject(slug);const title=p?`${p.title} — Financial Analysis & Modeling`:"Project";return {title,description:p?.description,openGraph:{title,description:p?.description,images:[]},twitter:{title,description:p?.description,images:[]}}}
export default async function ProjectPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=getProject(slug);if(!p)return <main><SiteHeader/><div className="page-intro shell"><h1>Project not found</h1></div></main>;return <main><SiteHeader/><header className="case-hero shell"><div><p className="eyebrow">Case study {p.number} · {p.year}</p><h1>{p.title}</h1><p className="case-sub">{p.subtitle}</p></div><div className="case-meta"><span className={`status ${p.status==="In Progress"?"live":""}`}>{p.status}</span><p>{p.description}</p></div></header><div className="case-layout shell"><aside className="toc"><p>Research areas</p>{p.topics.map(t=><span key={t}>{t}</span>)}</aside><article><Markdown body={p.body}/></article></div><SiteFooter/></main>}
